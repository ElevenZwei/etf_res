# SQL 设计说明  


## 几条规范  
如果一个数据列需要作为主键或者唯一索引的成员出现，那么这里要求它一定加上 not null 限制，如果想要输入空值，那么使用空的字符串，或者特殊的无意义数值表示。这是为了保护唯一索引的有效性。  
如果它只是可有可无的数据列，不是用来确保唯一性的，那么可以用 nullable column 。
尽量给所有非日志类的表格，也就是之后可能有改动的表格加上 inserted_at 和 updated_at 时间数据。这个对于之后的操作非常有意义。

